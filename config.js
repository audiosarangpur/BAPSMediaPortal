// Add a YouTube Data API v3 key here.
// Restrict the key to your portal's domain in Google Cloud Console.
// Leave blank to use the built-in fallback demo data.
window.BAPS_PORTAL_CONFIG = {
    YOUTUBE_API_KEY: ""
};
